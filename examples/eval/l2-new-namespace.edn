{:work-id "l2-new-namespace"
 :level :l2-new-namespace-wired-in
 :unit    "kotoba-lang/kotoba-delta"
 :repo    "kotoba-lang/kotoba-delta"
 :pin     "c8c405abda731e5889aff3ae2a18ac208b375bb5"
 :node    "naphtali"
 :requires #{:nbb :git}
 :test-cmd "kbb --backend sci --classpath src:test run-tests.cljs"
 :budget  {:max-turns 16 :max-tool-calls 40 :max-tokens 4000
           :deadline-ms 1200000 :test-timeout-ms 180000}
 :expect  {:must-touch ["src/delta/stats.cljc" "test/delta/stats_test.cljc" "run-tests.cljs"]
           :max-files 3
           :must-add ["ns delta.stats" "deftest" "delta.stats-test"]}
 :prompt
 "Task: add a new namespace with its own test namespace, and make sure the
  new tests actually run as part of this repository's test suite.

  1. Create src/delta/stats.cljc, namespace `delta.stats`, with a pure function
     (defn ops-per-file [ops]) returning a map from :op/file to how many ops in
     `ops` touch that file. Short docstring.
  2. Create test/delta/stats_test.cljc with a deftest covering two files and
     the empty case.
  3. Make sure the new tests are executed when the repository's test suite
     runs — a new test namespace that nothing runs is not a test.
  4. Run the test suite and make sure everything passes, including the
     existing tests."}
