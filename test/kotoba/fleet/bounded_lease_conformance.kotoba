(ns kotoba.fleet.bounded-lease-conformance
  (:require [kotoba.fleet.bounded-lease :as lease])
  (:export [main holder]))

(defn holder [data :vector-i64] :i64 (lease/holder data))
(defn main [] :i64 42)
